# Pixel Formula Roller — BA Review Archive

This document is retained only as an archive marker for an earlier planning pass.

Do not use it for current implementation decisions.

The canonical product direction is the Android-only specification in `docs/specs/app-spec.md` plus the current story files under `docs/stories/`.
